
<div class="card-body px-5 mb-2 border rounded">
  <div class="d-flex flex-start align-items-center">
      <img class="rounded-circle shadow-1-strong me-3"
        src="https://mdbcdn.b-cdn.net/img/Photos/Avatars/img%20(19).webp" alt="avatar" width="60"
        height="60" />
      <div>
        <h6 class="fw-bold text-primary mb-1"><a href="/users/show/<%= comment.user_id %>"><%= User.find(comment.user_id).full_name.blank? ? User.find(comment.user_id).email : User.find(comment.user_id).full_name %></a></h6>
        <p class="text-muted small mb-0">
          Shared publicly - <%= comment.created_at.strftime("%A %H:%M") %>
        </p>
      </div>
    </div>

    <p class="mt-3 mb-4 pb-2 text-black">
      <%= comment.text %>
    </p>
    <% if comment.user_id == current_user.id %>
      <div><%= button_to image_tag("trash.svg"), delete_comment_path(comment.tweet_id, comment.id), method: :delete, class: "btn btn-light border-none" %></div>
    <% end %>
  </div>
